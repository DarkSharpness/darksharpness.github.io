#include <bits/stdc++.h>
// #include <Dark/inout>
// #define int long long
// using ll = long long;
// using ull = unsigned long long;
using namespace std;


signed main() {
    static_assert(sizeof(long long) == sizeof(long)); // True
    static_assert(is_same_v <long long,long>); // False!
    return 0;
}